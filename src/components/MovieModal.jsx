function MovieModal({movie,onClose}){const image=movie.image?.original||movie.image?.medium;const rating=movie.rating?.average??"N/A";const year=movie.premiered?movie.premiered.slice(0,4):"N/A";return <div className="modal-backdrop" onClick={onClose}><div className="modal" onClick={e=>e.stopPropagation()}><button className="close-button" onClick={onClose}>✕</button>{image&&<img className="modal-image" src={image} alt={movie.name}/>}<div className="modal-content"><h2>{movie.name}</h2><div className="modal-meta"><span>⭐ Rating: {rating}</span><span>📅 Release: {year}</span></div><p><strong>Genre:</strong> {movie.genres?.length?movie.genres.join(", "):"N/A"}</p><h3>Overview</h3><div className="summary" dangerouslySetInnerHTML={{__html:movie.summary||"No summary available."}}/><button className="close-bottom" onClick={onClose}>Close</button></div></div></div>}
export default MovieModal;