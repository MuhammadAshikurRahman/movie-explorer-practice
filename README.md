# Movie Explorer Practice

React + Vite practice project using the public TVMaze API.

Run:

```bash
npm install
npm run dev
```

Main concepts: Components, Props, useState, useEffect, events, map(), conditional rendering, fetch(), API integration, modal and responsive CSS Grid.